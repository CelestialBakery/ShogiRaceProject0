public enum Enum_PieceType
{
    King,   // ‰¤«(Ou)
    Rook,   // ”òÔ(Hisha)
    Bishop, // Šps(Kaku)
    Gold,   // ‹à«(Kin)
    Silver, // ‹â«(Gin)
    Knight, // Œj”n(Kei)
    Lance,  // Ô(Kyou)
    Pawn,   // •à•º(Hu)
    Max     // Piece Count
}
